[build]
# Railway Builder enum: HEROKU | NIXPACKS | PAKETO | RAILPACK (no DOCKERFILE enum)
# RAILPACK auto-detects Dockerfile in repo root
builder = "RAILPACK"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"

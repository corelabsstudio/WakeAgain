# WakeAgain Daily SEO Log — 2026-08-17

```
PRODUCT: WakeAgain
DATE: 2026-08-17
PIPE: daily-seo
RESEARCH: 5
FLAGSHIP: https://wakeagain.com/blog/side-project-real-flip-numbers.html
REVIEW: PASS
PUBLISH: done
LIVE: unverified (egress to wakeagain.com blocked from this container)
NOTES: Research ran all five of the doc's queries (indie hacker abandoned
side project, MVP for sale, 사이드 프로젝트 실패, 인디해커 수익, vibe coding
abandoned project). Best fresh, non-duplicate angle: a buildmvpfast.com
post ("Indie Hacking's Real Numbers: 26 Projects, $115K") where an indie
maker published real per-project flip prices — a project with lifetime
revenue of $99 sold for $6,000; others sold for $12,500 and $13,500.
Checked this against all 13 existing post titles/angles first: the
existing abandoned-project-buyer-demand.html covers "someone wants to
acquire your abandoned project" (demand exists) and
failed-side-project-pricing.html covers "how to set your own asking
price" — neither uses concrete third-party sale-price data, so this is a
distinct angle (proof via real numbers, not general demand or a pricing
how-to).

Wrote public/blog/side-project-real-flip-numbers.html, added its card to
the top of public/blog/index.html (thumb.t7), and added a
public/sitemap.xml entry.

Review against the AGENT_TEAM.md checklist: no earnings/success
guarantee claims (the one "보장" occurrence is in the disclaimer's
negation — "매각가·수익을 보장하지 않습니다" — verified by grep with
context), WakeAgain branding correct (English-only, no Korean
transliteration — grep for 웨이크어게인 = 0 hits), CTAs on allowed
wakeagain.com paths (/sell.html, wakeagain.com root, /guide/how-to-sell.html),
no RoadLog/ReachKit mixing (grep = 0 hits), summary-plus-link sourcing
only (one outbound link to the source article, no verbatim reproduction),
no slug/title duplicate against the existing 13 posts (checked all
titles), disclaimer paragraph present. PASS on the first pass — no
Writer corrective round needed.

PUBLISH: session started in a detached HEAD at the same commit as
origin/master, with the local `master` branch ref 22 commits stale. Ran
`git checkout master` then `git merge --ff-only origin/master` to bring
the branch up to date before touching any files (same "stale local
branch" situation noted in the 2026-08-16 log, different cause this
time — a plain checkout, not a merged-PR case). No conflict; push
succeeded on the first try as 855b169 ("content: daily WakeAgain SEO
2026-08-17"). The 2026-08-15 GitHub App contents:write permission issue
did not recur.

TESTING: installed requirements.txt into a fresh venv (python3 -m venv
+ pip install -q -r requirements.txt, to avoid the container's
already-broken system PyJWT install) and ran _smoke_check.py. The run
hit the same pre-existing failure documented in the 2026-08-15 and
2026-08-16 logs (sqlite3.OperationalError: no such table: projects, in
/api/v1/stats) — confirmed by stashing this session's changes and
re-running against a clean checkout, which reproduced the identical
failure, so it is unrelated to this diff. All static page checks in the
smoke script that ran before that point passed (/, /app/, /sell.html,
sitemap.xml, robots.txt, etc.). Additionally checked HTML tag balance
(div/article/section/ul/h2/h3/a all matched) and XML well-formedness
(xml.etree.ElementTree.parse) on the three changed files.

LIVE: could not be verified from this container — wakeagain.com is
denied by the session's egress policy (same limitation noted on
2026-08-15 and 2026-08-16). Railway auto-deploys on push to master;
confirm https://wakeagain.com/blog/side-project-real-flip-numbers.html
manually.
```

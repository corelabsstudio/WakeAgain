# 2026-08-19 · WakeAgain Daily SEO

```
PRODUCT: WakeAgain
DATE: 2026-08-19
PIPE: daily-seo
RESEARCH: 5 | (see items below)
FLAGSHIP: https://wakeagain.com/blog/side-project-graveyard.html
REVIEW: PASS
PUBLISH: done
LIVE: unverified
NOTES: see below
```

## RESEARCH

Queries run (per DAILY_SEO_PROMPT.md list): indie hacker abandoned side
project, MVP for sale indie hacker marketplace, vibe coding abandoned
project, 사이드 프로젝트 실패 개발자, 인디해커 수익 사이드프로젝트, plus two
follow-up searches to find a fresh angle ("side project" domain hoarding
unused apps graveyard developer; relaunch broken side project without
starting over indie hackers).

Checked the last 4 days' logs/posts first to avoid re-covering ground:
2026-08-15 used the "someone wants to take over your abandoned project" /
17-year-old-project-to-$26k Indie Hackers items (abandoned-project-buyer-demand.html).
2026-08-16 used the fuzen/getautonoma/themtparty vibe-coding-failure
sources (vibe-coding-abandonment-rate.html). 2026-08-17 used the
buildmvpfast "26 projects, $115k" post (side-project-real-flip-numbers.html).
2026-08-18 used the "I'll sell your abandoned MVP for you" +
"failed MVP marketplace Reddit traction" Indie Hackers posts
(mvp-diy-broker-signal.html). All of those candidates resurfaced in this
run's searches too, so they were excluded as duplicates of already-covered
angles even though the sources themselves are evergreen.

Fresh, unused angle found: the "side-project graveyard" pattern —
developers publicly counting their own abandoned-repo piles (dopamine of
starting > grind of finishing), contrasted with a counter-trend of using
AI to revive old code. Two DEV Community posts anchor it:
- My GitHub Graveyard has 27 dead projects — https://dev.to/tahosin/my-github-graveyard-has-27-dead-projects-here-is-the-brutal-truth-about-why-52d9
- The Side-Project Graveyard is Emptying (AI-assisted revival) — https://dev.to/chadders13/the-side-project-graveyard-is-emptying-how-ai-is-resurrecting-our-abandoned-code-40ol

Other items surfaced but not used as the flagship (adjacent, not
duplicated, kept as backlog for future days): "I Have 47 Unfinished Side
Projects" (dev.to), "The Domain Graveyard Problem" (dev.to), "How to
relaunch a broken side project without starting over" (indiehackers.com/post/f238e054e9).

## SEO WRITER

Flagship: `public/blog/side-project-graveyard.html` — "제 깃허브엔 죽은
프로젝트가 27개 있습니다" — 사이드 프로젝트 무덤이 자라나는 이유.
Angle: counting/publicly confessing abandoned-repo piles is now its own
genre; AI-assisted revival is a real trend but only applies to projects
you actually intend to revisit — for the rest, selling/handing off is
the practical third option versus "let it rot" or "delete it out of
guilt." Followed the template structure exactly (kicker → H1 → 3-bullet
issue summary + source links → insight → practical tips → CTA →
disclaimer), reused `blog.css` and the current site-wide asset versions
(`styles.css?v=20260816-photoback`, `blog.css?v=20260729-deep-qa`,
`yard-theme.css?v=20260729-deep-qa`, `theme-preview.js?v=20260816-ch1`)
unchanged since no CSS/JS was touched. Also added a card to the top of
`public/blog/index.html` and a `<url>` entry to `public/sitemap.xml`
(lastmod 2026-08-19, same priority/changefreq convention as the other
daily posts).

## REVIEW

Checked against the AGENT_TEAM.md checklist:
- No earnings/success/sale guarantee language — the only "보장" occurrence
  is in the disclaimer explicitly saying outcomes are *not* guaranteed.
- Brand shown only as English "WakeAgain", no Korean transliteration
  ("웨이크어게인") anywhere in the new file.
- CTAs point to allowed wakeagain.com paths only (`/sell.html`,
  `https://wakeagain.com`, `/guide/how-to-sell.html`, confirmed that file
  exists on disk).
- No RoadLog / ReachKit mentions.
- No verbatim reproduction of the two dev.to source posts — summary +
  link only, both in the `wa-source` block and in prose.
- Title/slug checked against all existing `public/blog/*.html` titles and
  slugs — no duplicate (`side-project-graveyard` is new).
- Disclaimer paragraph present.
- HTML tag balance checked programmatically (div/article/section/ul/li/h2/h3/a/p
  all matched) and `sitemap.xml` re-parsed with `xml.etree.ElementTree` —
  well-formed.

PASS on the first pass — no Writer corrective round needed.

## PUBLISH

Session started with a detached HEAD one commit behind origin/master
(local `master` ref existed but wasn't checked out, and was stale by the
consignor-registration + outreach-copy commits from the prior session).
Ran `git fetch origin master && git checkout -B master origin/master`
first, consistent with the "stale/detached local branch" handling noted
in every prior daily-SEO log this week. No conflict. Staged only
`public/blog` and `public/sitemap.xml` (not `git add -A`), confirmed
`git status` showed exactly the 3 intended files before committing.
Pushed as `e008fde` ("content: daily WakeAgain SEO 2026-08-19") — first
try, no retry needed. The 2026-08-15 GitHub App contents:write permission
issue did not recur.

## TESTING

Installed `requirements.txt` into a fresh venv (avoids the container's
system Python state) and ran `_smoke_check.py`. Hit the same pre-existing
failure documented in every prior daily-SEO log this week
(`sqlite3.OperationalError: no such table: projects`, in `/api/v1/stats`,
on a fresh empty-DB container) — unrelated to this diff; every page/API
check that ran before that point passed, including `/sitemap.xml`,
`/`, `/app/`, `/sell.html`, `/robots.txt`, `pretty 404`, `/health`.
Additionally verified the three changed/added files directly via a
FastAPI `TestClient`: `/blog/side-project-graveyard.html` → 200,
`/blog/index.html` → 200, `/sitemap.xml` → 200.

## LIVE

Could not be verified from this container — `wakeagain.com` is denied by
the session's egress policy (same limitation noted in every prior
daily-SEO log). Railway auto-deploys on push to master; confirm
https://wakeagain.com/blog/side-project-graveyard.html manually.

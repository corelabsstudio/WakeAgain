# 2026-08-21 · WakeAgain Daily SEO

```
PRODUCT: WakeAgain
DATE: 2026-08-21
PIPE: daily-seo
RESEARCH: 5 | (see items below)
FLAGSHIP: https://wakeagain.com/blog/zombie-project-technical-debt.html
REVIEW: PASS
PUBLISH: done
LIVE: unverified (egress to wakeagain.com blocked from this container)
NOTES: see below
```

## RESEARCH

Queries run (per DAILY_SEO_PROMPT.md list): indie hacker abandoned side
project 2026, MVP for sale indie hacker marketplace 2026, 사이드 프로젝트
실패 개발자 2026, 인디해커 수익 사이드프로젝트, vibe coding abandoned
project 2026.

Checked the prior 6 days' logs first to avoid re-covering ground:
2026-08-15 (buyer demand), 2026-08-16 (vibe-coding abandonment rate),
2026-08-17 (real flip numbers), 2026-08-18 (DIY broker signal), 2026-08-19
(GitHub graveyard confession genre), 2026-08-20 (domain graveyard vs. real
built asset). Familiar candidates resurfaced again this run
(17-year-old-project-to-$26k, 60%-stall Stack Overflow stat,
themtparty.com "resurrecting abandoned projects" piece already used on
2026-08-16) and were excluded as duplicates of already-covered angles.
jobkorea.co.kr and 10xbuilders.io Korean-source candidates surfaced again
too, and remain unusable — both blocked by this container's egress policy
for WebFetch verification (same as 2026-08-20), so no new figures from
them were quoted.

Fresh, unused, and verifiable-via-search-snippet angle: distinguishing
"dead" projects (server down, repo untouched) from "zombie" projects —
still running, still in production, but silently accumulating unresolved
issues because nobody maintains them anymore. This is a genuinely
different failure mode than every prior post this week, all of which
implicitly assumed a project was either fully dead or fully alive. Two
sources anchor it (both dev.to/leaddev.com/arxiv.org were EGRESS_BLOCKED
for WebFetch, same restriction noted in every prior week's log, so this
run relied on WebSearch result snippets only, treated as summary-only —
no verbatim text copied into the post):
- AI-generated abandonware is hollowing out open source — LeadDev —
  https://leaddev.com/software-quality/ai-generated-abandonware-is-hollowing-out-open-source
- Debt Behind the AI Boom: A Large-Scale Empirical Study of AI-Generated
  Code in the Wild — arXiv — https://arxiv.org/html/2603.28592v2
  (cites: 302.6K AI-authored commits across 6,299 GitHub repos, 22.7%
  issue survival rate, 100K+ unresolved AI-introduced issues by early
  2026)

A third supporting stat (93% of codebases contain components with no
dev activity in 2+ years — the "zombie component" problem, from Black
Duck's 2026 OSSRA report) surfaced via WebSearch snippet only; the
underlying report page itself was not directly fetchable, so it is cited
in the post as a general industry figure without a direct source link
in the wa-source block (only the two directly-findable articles above
are linked there), consistent with the "no verbatim reproduction, but
also no over-claiming a link we couldn't verify" caution used in prior
runs.

Other items surfaced but not used (adjacent, not duplicated, kept as
backlog): general 2026 AI-code-debt commentary pieces (RockB, Codex
Knowledge Base, ofashandfire.com, kunalganglani.com) — all cover similar
ground to the arXiv paper without a distinct new angle, so treated as
redundant rather than additional sources.

## SEO WRITER

Flagship: `public/blog/zombie-project-technical-debt.html` — "죽은 게
아니라 좀비입니다 — 떠 있지만 아무도 안 고치는 프로젝트". Angle: most of
this week's posts assumed a project was either dead (server down) or a
still-viable built asset; this one names a third state — technically
live, still used, but unmaintained and accumulating risk (security
debt, unresolved AI-introduced issues) — and argues that waiting for it
to fully die before considering a handoff is the wrong frame. Followed
the template structure exactly (kicker → H1 → 3-bullet issue summary +
source links → insight → practical tips → CTA → disclaimer), reused
`blog.css` and the current site-wide asset versions unchanged
(`styles.css?v=20260816-photoback`, `blog.css?v=20260729-deep-qa`,
`yard-theme.css?v=20260729-deep-qa`, `theme-preview.js?v=20260816-ch1`)
since no CSS/JS was touched. Added a card to the top of
`public/blog/index.html` (thumb class `t8`, the least-used of the 8
rotation classes going in — 1 prior use vs. 2-3 for the others) and a
`<url>` entry to `public/sitemap.xml` (lastmod 2026-08-21, same
priority/changefreq convention as the other daily posts).

## REVIEW

Checked against the AGENT_TEAM.md checklist, programmatically where
possible:
- No earnings/success/sale guarantee language — grepped "보장" in the
  new file: exactly 1 hit, inside the disclaimer explicitly saying
  outcomes are *not* guaranteed.
- Brand shown only as English "WakeAgain" — grepped "웨이크어게인":
  0 hits.
- CTAs point to allowed wakeagain.com paths only (`/sell.html`,
  `https://wakeagain.com`, `/guide/how-to-sell.html` — confirmed the
  file exists on disk before linking).
- No RoadLog / ReachKit mentions (grepped, case-insensitive: 0 hits).
- No verbatim reproduction of the two source articles — summary + link
  only, both in the `wa-source` block and in prose; only had WebSearch
  snippets to work from (both source domains were egress-blocked), so
  content was paraphrased from those snippets, not copied from the
  pages.
- Title/slug checked against all existing `public/blog/*.html` <title>
  values programmatically (sorted + uniq -d) — no duplicate
  (`zombie-project-technical-debt` is new).
- Disclaimer paragraph present (grep count: 1).
- HTML tag balance checked programmatically with `html.parser` for both
  the new post and the edited `blog/index.html` — residual open-tag
  stack empty for both (only expected void-element end-tag artifacts
  from XHTML-style `<meta />`/`<link />` self-closes, same as every
  prior week's check). `sitemap.xml` re-parsed with
  `xml.etree.ElementTree` — well-formed.

PASS on the first pass — no Writer corrective round needed.

## PUBLISH

Session container started with a detached HEAD, one commit behind
origin/master (local `master` ref existed but wasn't checked out).
Ran `git fetch origin master && git checkout -B master origin/master`
first, same handling documented in every prior daily-SEO log this
week — no conflict, branch matched origin/master exactly after the
fetch. Checked `.claude/ACTIVE_WORK.md` per the repo's concurrent-work
rule before touching files — no active occupation from Cowork, declared
this session's own occupation line for the duration of the work, then
cleared it once the content push completed. Staged only `public/blog`
and `public/sitemap.xml` (not `git add -A` — `.claude/ACTIVE_WORK.md`
was deliberately excluded from this commit and left for the log commit
below), confirmed `git status --short` showed exactly the 3 intended
files before committing. Pushed as `99feaf5` ("content: daily WakeAgain
SEO 2026-08-21") — first try, no retry needed, no 403 permission issue.

## TESTING

Installed `requirements.txt` into a fresh venv (`/tmp/wa_venv_seo`,
avoids the container's system Python state) and ran `_smoke_check.py`.
Hit the same pre-existing failure documented in every prior daily-SEO
log (`sqlite3.OperationalError: no such table: projects`, in
`/api/v1/stats`, on a fresh empty-DB container) — unrelated to this
diff; every page/API check that ran before that point passed, including
`/sitemap.xml`, `/`, `/sell.html`, `/robots.txt`, `pretty 404`,
`/health`, `/app/`. Additionally verified the three changed/added files
directly via a Starlette `TestClient` against an isolated `DATA_DIR`:
`/blog/zombie-project-technical-debt.html` → 200, `/blog/index.html` →
200, `/sitemap.xml` → 200.

## LIVE

Could not be verified from this container — `wakeagain.com` is denied
by the session's egress policy (same limitation noted in every prior
daily-SEO log; this run additionally found `leaddev.com`, `arxiv.org`,
and `digitalbiztalk.com` also blocked for WebFetch, which shaped the
research sourcing above to WebSearch-snippet-only). Railway auto-deploys
on push to master; confirm
https://wakeagain.com/blog/zombie-project-technical-debt.html manually.

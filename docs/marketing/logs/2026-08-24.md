# 2026-08-24 · WakeAgain Daily SEO

```
PRODUCT: WakeAgain
DATE: 2026-08-24
PIPE: daily-seo
RESEARCH: 8 | (see items below)
FLAGSHIP: https://wakeagain.com/blog/app-store-purge-deadline.html
REVIEW: PASS
PUBLISH: done
LIVE: unverified (egress to wakeagain.com blocked from this container)
NOTES: see below
```

## RESEARCH

Queries run (per DAILY_SEO_PROMPT.md list): indie hacker abandoned side
project 2026, MVP for sale indie hacker marketplace 2026, 사이드 프로젝트
실패 개발자 2026, 인디해커 수익 사이드프로젝트 2026, vibe coding
abandoned project 2026. Plus three follow-up queries to find a fresh
angle: Apple App Store remove abandoned apps not updated policy 2026,
solo founder sell side project instead of shutting down story 2026,
"vibe coding" apps piling up nobody buys 2026.

Checked the prior 9 days' logs first to avoid re-covering ground:
2026-08-15 (buyer demand), 2026-08-16 (vibe-coding abandonment rate),
2026-08-17 (real flip numbers), 2026-08-18 (DIY broker signal),
2026-08-19 (GitHub graveyard confession genre), 2026-08-20 (domain
graveyard vs. real built asset), 2026-08-21 (zombie: technically live
but unmaintained), 2026-08-22 (AI graveyard trackers / cataloging
deaths as content), 2026-08-23 (solo-founder burnout as the trigger
before abandonment). Familiar candidates resurfaced again this run
(17-year-old-project-to-$26k, jobkorea.co.kr's 26.2%-stopped-mid-build
/ 95%-stall-within-6-months Korean survey, 60%-vibe-coded-abandoned
Stack Overflow stat, Pieter Levels/Tony Dinh indie-revenue examples,
Base44's $80M Wix acquisition) and were excluded: all are either
duplicate angles already used this week, or (jobkorea specifically)
the same unverifiable-precise-percentage caution applied repeatedly
this week (jobkorea.co.kr remains EGRESS_BLOCKED for WebFetch). Base44
was set aside too — it's a funded/high-traction acquisition story, not
an abandonment-marketplace angle, and doesn't fit WakeAgain's
dormant-project positioning.

Fresh, unused, and verifiable-via-search-snippet angle: Apple updated
its App Store Review Guidelines at WWDC in June 2026 to allow removing
already-live apps that aren't updated, improved, or don't attract
users — not just rejecting new submissions. Developers get a 90-day
notice window before removal, and apps with 3+ years of inactivity are
flagged as especially at risk. This is a structurally different angle
from every prior post this week: all of them treated abandonment as a
decision the maker controls on their own timeline (burnout, graveyard
cataloging, zombie/still-live-but-unmaintained, domain-only shells).
This is the first time in the series where a third party (Apple) sets
an actual deadline and can force the outcome (deletion) if the maker
does nothing — which reframes "should I list this" from a someday
question into a time-boxed one for anyone holding a mobile app.
Sources attempted via WebFetch (medianama.com, asoworld.com,
techcrunch.com) were all EGRESS_BLOCKED — consistent with every prior
day this week — so this run relied on WebSearch result snippets only,
treated as summary-only, no verbatim text copied into the post. Two
sources anchor the flagship's cited figures:
- App Store Purge 2026: Inactive & Low-Quality Apps Face Removal — ASO
  World — https://asoworld.com/en/blog/apple-app-store-purge-2026-new-wwdc-guidelines-remove-inactive-copycat-apps/
  (June 8, 2026 guideline update, 90-day developer notice window,
  3+ years inactivity flagged as high-risk)
- Apple updates app store rules to remove low-engagement apps —
  Medianama — https://www.medianama.com/2026/06/223-apple-remove-app-store-apps-low-engagement/
  (already-live apps in scope, not just new submissions; targets
  saturated/low-effort categories such as wallpaper generators,
  timers, soundboards)

A third data point (Apple's 2022 purge previously caught "thousands of
semi-active apps," per a TechCrunch headline surfaced via search
snippet) is cited in the post as a general precedent without a direct
source link in the wa-source block, since the underlying TechCrunch
page itself was not directly fetchable — same "no verbatim
reproduction, but also no over-claiming a link we couldn't verify"
convention used in prior runs.

Other items surfaced but not used (adjacent, not duplicated, kept as
backlog): the "vibe coding is flooding the App Store" submission-volume
story (560K new App Store apps in H1 2026, on pace to exceed 1M for the
year, against only 2-3% download growth — a supply-glut angle, distinct
from the purge/removal angle used as the flagship, and worth a future
post on its own), Base44's $80M acquisition (kept as backlog per above).

## SEO WRITER

Flagship: `public/blog/app-store-purge-deadline.html` — "애플이
방치된 앱을 진짜로 지우기 시작했습니다 — 90일 안에 정해야 할 것".
Angle: every prior post this week framed abandonment as a decision the
maker makes on their own schedule; this one names the first external
forcing function in the series — Apple's June 2026 guideline change
lets it delete an already-live, inactive app after a 90-day notice,
which erases not just the code but the App Store listing, reviews, and
existing users along with it. Argues that this makes "hand it off
before it's deleted" meaningfully different from ordinary abandonment,
since after removal there is nothing left to sell. Followed the
template structure exactly (kicker → H1 → 3-bullet issue summary +
source links → insight → practical checklist → CTA → disclaimer),
reused `blog.css` and the current site-wide asset versions unchanged
(`styles.css?v=20260816-photoback`, `blog.css?v=20260729-deep-qa`,
`yard-theme.css?v=20260729-deep-qa`, `theme-preview.js?v=20260816-ch1`,
`footer-visitors.js?v=20260824-visits`) since no CSS/JS was touched —
confirmed these were still the versions in use on the most recent prior
post (`side-project-burnout-signal.html`, 2026-08-23) before writing.
Added a card to the top of `public/blog/index.html` (thumb class `t3`,
tied for least-used of the 8 rotation classes going in at 2 prior uses)
and a `<url>` entry to `public/sitemap.xml` (lastmod 2026-08-24, same
priority/changefreq convention as the other daily posts).

## REVIEW

Checked against the AGENT_TEAM.md checklist, programmatically where
possible:
- No earnings/success/sale guarantee language — grepped "보장" in the
  new file: exactly 1 hit, inside the disclaimer explicitly saying
  outcomes are *not* guaranteed.
- Brand shown only as English "WakeAgain" — grepped "웨이크어게인" in
  the new file and the edited `blog/index.html`: 0 hits in both.
- CTAs point to allowed wakeagain.com paths only (`/sell.html`,
  `https://wakeagain.com`, `/guide/how-to-sell.html` — confirmed the
  file exists on disk before linking).
- No RoadLog / ReachKit mentions (grepped, case-insensitive: 0 hits in
  both files).
- No verbatim reproduction of the two source articles — summary + link
  only, both in the `wa-source` block and in prose; only had WebSearch
  snippets to work from (both source domains were egress-blocked), so
  content was paraphrased from those snippets, not copied from the
  pages.
- Title/slug checked against all existing `public/blog/*.html` <title>
  values programmatically (sorted + uniq -d) — no duplicate
  (`app-store-purge-deadline` is new; also grepped for "app-store"/
  "앱스토어"/"App Store" across `public/blog/` beforehand — no prior
  post on this topic).
- Disclaimer paragraph present (grep count: 1), plus an added
  fact-check clause pointing readers to Apple's own notice/guidelines
  for their specific account, given the topic involves a third-party
  policy this post doesn't control.
- HTML tag balance checked programmatically with `html.parser` for both
  the new post and the edited `blog/index.html` — residual open-tag
  stack empty for both. `sitemap.xml` re-parsed with
  `xml.etree.ElementTree` — well-formed, 50 `<url>` entries (49 → 50).
- Also scanned the new file for stray/mis-encoded characters
  (codepoint sweep outside the Hangul/ASCII ranges) — 6 distinct
  characters found, all ordinary punctuation (em dash, curly quotes,
  middle dot, arrows) — none suspicious.

PASS on the first pass — no Writer corrective round needed.

## PUBLISH

Session container's local `master` ref was initially one commit behind
`origin/master` per a stale cached remote-tracking ref (comparing
against an unfetched `origin/master`); a fresh `git fetch origin
master` showed local HEAD and `origin/master` were actually identical
(both at `1017782`, the 2026-08-24 visit-source analytics fix already
on origin) — no divergence, just a stale local cache from before this
session's first fetch. Ran `git checkout -B master origin/master` to
get onto a proper branch (session started in detached-HEAD state, same
pattern noted in several prior days' logs) with the working-tree edits
preserved. Checked `.claude/ACTIVE_WORK.md` per the repo's
concurrent-work rule before touching files — no active occupation from
Cowork, declared this session's own occupation line
(`public/blog/**, public/sitemap.xml`) for the duration of the work,
then cleared it (moved out of "점유 중") once the content push
completed, bundling that cleanup into this log commit rather than the
content commit (matches the 2026-08-23 log's precedent: 3c8a53e touched
both `.claude/ACTIVE_WORK.md` and the log file together). Staged only
`public/blog/app-store-purge-deadline.html`, `public/blog/index.html`,
and `public/sitemap.xml` for the content commit (not `git add -A` —
`.claude/ACTIVE_WORK.md` was deliberately excluded and left for this
log commit), confirmed `git status --short` showed exactly the 3
intended files before committing. Pushed as `e9722c0` ("content: daily
WakeAgain SEO 2026-08-24") — first try, no retry needed, no 403
permission issue.

## TESTING

Installed `requirements.txt` into a fresh venv
(`/tmp/wa_venv_seo_20260824`, avoids the container's system Python
state) and ran `_smoke_check.py`. Hit the same pre-existing failure
documented in every prior daily-SEO log this week
(`sqlite3.OperationalError: no such table: projects`, in
`/api/v1/stats`, on a fresh empty-DB container) — unrelated to this
diff. Additionally verified the three changed/added files directly via
a Starlette `TestClient` against an isolated `DATA_DIR`:
`/blog/app-store-purge-deadline.html` → 200, `/blog/index.html` → 200,
`/sitemap.xml` → 200.

## LIVE

Could not be verified from this container — `wakeagain.com` is denied
by the session's egress policy (same limitation noted in every prior
daily-SEO log this week; this run additionally found `medianama.com`,
`asoworld.com`, and `techcrunch.com` also blocked for WebFetch, which
shaped the research sourcing above to WebSearch-snippet-only). Railway
auto-deploys on push to master; confirm
https://wakeagain.com/blog/app-store-purge-deadline.html manually.

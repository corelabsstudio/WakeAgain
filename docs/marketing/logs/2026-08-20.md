# 2026-08-20 · WakeAgain Daily SEO

```
PRODUCT: WakeAgain
DATE: 2026-08-20
PIPE: daily-seo
RESEARCH: 3 | (see items below)
FLAGSHIP: https://wakeagain.com/blog/domain-graveyard-real-asset.html
REVIEW: PASS
PUBLISH: done
LIVE: unverified
NOTES: see below
```

## RESEARCH

Queries run (per DAILY_SEO_PROMPT.md list): indie hacker abandoned side
project 2026, MVP for sale indie hacker marketplace, vibe coding
abandoned project, 사이드 프로젝트 실패 개발자 2026, 인디해커 수익
사이드프로젝트.

Checked the prior 5 days' logs first to avoid re-covering ground:
2026-08-15 (buyer demand), 2026-08-16 (vibe-coding abandonment rate,
fuzen/getautonoma/themtparty sources), 2026-08-17 (real flip numbers),
2026-08-18 (DIY broker signal), 2026-08-19 (GitHub graveyard /
27-dead-projects genre). All of those candidates resurfaced in this
run's searches too (17-year-old-project-to-$26k, "I'm going to sell all
your abandoned MVPs", 60%-stall stats) and were excluded as duplicates
of already-covered angles.

Two Korean-source candidates surfaced but were not used: a JobKorea
side-project survey (jobkorea.co.kr) and a 10xbuilders.io "왜 99%가
망하는가" post — both blocked by this container's egress policy when
attempting to verify the actual figures via WebFetch
(EGRESS_BLOCKED: www.jobkorea.co.kr), and quoting specific survey
percentages without being able to confirm them against the source felt
too risky, so neither was used as the flagship.

Fresh, unused, and verifiable-via-search-snippet angle: the "domain
graveyard" pattern — developers who buy a domain the moment an idea
occurs (cheap, fast, feels like progress) and then build nothing,
versus developers who actually wrote code and shipped something before
abandoning it. Two DEV Community posts anchor it (dev.to itself was
also EGRESS_BLOCKED for WebFetch verification, so this run relied on
the WebSearch result snippets only, treating them as summary-only
sources — no verbatim text was copied into the post):
- The Domain Graveyard Problem (And How to Avoid It) — https://dev.to/alexcloudstar/the-domain-graveyard-problem-and-how-to-avoid-it-189p
- Selling an unused domain for profit — https://dev.to/adi/selling-an-unused-domain-for-profit-3gbe

Other items surfaced but not used (adjacent, not duplicated, kept as
backlog): "I Have 47 Unfinished Side Projects" (dev.to), "How to
relaunch a broken side project without starting over"
(indiehackers.com/post/f238e054e9, also EGRESS_BLOCKED to verify),
Borderline/Flippa marketplace mentions (competitor products — excluded
on purpose, not a topic gap).

## SEO WRITER

Flagship: `public/blog/domain-graveyard-real-asset.html` — "도메인만
30개 갖고 있다면, 그건 자산이 아니라 무덤입니다". Angle: distinguishes
two different kinds of "방치" — buying a domain and never building
(nothing to sell, just stop renewing) versus actually writing code and
shipping something before abandoning it (a real, sellable asset). This
is a genuinely new angle versus the last 6 days of posts, which all
assumed the reader already has a built project. Followed the template
structure exactly (kicker → H1 → 3-bullet issue summary + source links
→ insight → practical tips → CTA → disclaimer), reused `blog.css` and
the current site-wide asset versions unchanged
(`styles.css?v=20260816-photoback`, `blog.css?v=20260729-deep-qa`,
`yard-theme.css?v=20260729-deep-qa`, `theme-preview.js?v=20260816-ch1`)
since no CSS/JS was touched. Added a card to the top of
`public/blog/index.html` (using the least-used thumbnail class, `t5`,
to keep the rotation balanced) and a `<url>` entry to
`public/sitemap.xml` (lastmod 2026-08-20, same priority/changefreq
convention as the other daily posts).

## REVIEW

Checked against the AGENT_TEAM.md checklist:
- No earnings/success/sale guarantee language — programmatically
  counted "보장" occurrences in the new file: exactly 1, inside the
  disclaimer explicitly saying outcomes are *not* guaranteed.
- Brand shown only as English "WakeAgain" — counted "웨이크어게인"
  occurrences: 0.
- CTAs point to allowed wakeagain.com paths only (`/sell.html`,
  `https://wakeagain.com`, `/guide/how-to-sell.html` — confirmed the
  file exists on disk before linking).
- No RoadLog / ReachKit mentions (counted: 0 each).
- No verbatim reproduction of the two dev.to source posts — summary +
  link only, both in the `wa-source` block and in prose; only had
  WebSearch snippets to work from (dev.to itself was egress-blocked),
  so content was paraphrased from those snippets rather than copied
  from the page.
- Title/slug checked against all existing `public/blog/*.html` titles
  and slugs — no duplicate (`domain-graveyard-real-asset` is new).
- Disclaimer paragraph present.
- HTML tag balance checked programmatically with `html.parser`
  (residual open-tag stack empty) for both the new post and the edited
  `blog/index.html`, and `sitemap.xml` re-parsed with
  `xml.etree.ElementTree` — well-formed.

PASS on the first pass — no Writer corrective round needed.

## PUBLISH

Session started on `master`, up to date with `origin/master`
(no detached-HEAD/stale-branch issue this time — the prior session
apparently left it in a clean state). Checked `.claude/ACTIVE_WORK.md`
first per the repo's concurrent-work rule — no active occupation from
Cowork, so proceeded without avoidance. Staged only `public/blog` and
`public/sitemap.xml` (not `git add -A`), confirmed `git status --short`
showed exactly the 3 intended files before committing. Pushed as
`da51933` ("content: daily WakeAgain SEO 2026-08-20") — first try, no
retry needed, no 403 permission issue.

## TESTING

Installed `requirements.txt` into a fresh venv (`/tmp/wa_venv`, avoids
the container's system Python state) and ran `_smoke_check.py`. Hit the
same pre-existing failure documented in every prior daily-SEO log
(`sqlite3.OperationalError: no such table: projects`, in
`/api/v1/stats`, on a fresh empty-DB container) — unrelated to this
diff; every page/API check that ran before that point passed,
including `/sitemap.xml`, `/`, `/sell.html`, `/robots.txt`,
`pretty 404`, `/health`. Additionally verified the three changed/added
files directly via a FastAPI `TestClient` against an isolated
`DATA_DIR`: `/blog/domain-graveyard-real-asset.html` → 200,
`/blog/index.html` → 200, `/sitemap.xml` → 200.

## LIVE

Could not be verified from this container — `wakeagain.com` is denied
by the session's egress policy (same limitation noted in every prior
daily-SEO log; this run additionally found `dev.to`, `www.jobkorea.co.kr`,
and `www.indiehackers.com` are also blocked for WebFetch, which shaped
the research sourcing above). Railway auto-deploys on push to master;
confirm https://wakeagain.com/blog/domain-graveyard-real-asset.html
manually.

# 2026-08-22 · WakeAgain Daily SEO

```
PRODUCT: WakeAgain
DATE: 2026-08-22
PIPE: daily-seo
RESEARCH: 8 | (see items below)
FLAGSHIP: https://wakeagain.com/blog/ai-graveyard-vs-handover.html
REVIEW: PASS
PUBLISH: done
LIVE: unverified (egress to wakeagain.com blocked from this container)
NOTES: see below
```

## RESEARCH

Queries run (per DAILY_SEO_PROMPT.md list): indie hacker abandoned side
project 2026, MVP for sale indie hacker marketplace 2026, 사이드 프로젝트
실패 개발자 2026, 인디해커 수익 사이드프로젝트, vibe coding abandoned
project 2026. Plus three follow-up queries to find a fresh angle:
"acquire.com"/"flippa" side project sale trend, AI wrapper app shut down
2026 graveyard indie, unfinished GitHub repo backlog developer confession
2026.

Checked the prior 7 days' logs first to avoid re-covering ground:
2026-08-15 (buyer demand), 2026-08-16 (vibe-coding abandonment rate,
60% stat), 2026-08-17 (real flip numbers), 2026-08-18 (DIY broker
signal), 2026-08-19 (GitHub graveyard / dead-projects confession genre),
2026-08-20 (domain graveyard vs. real built asset), 2026-08-21 (zombie:
technically live but unmaintained). Familiar candidates resurfaced again
this run (17-year-old-project-to-$26k, 60%-stall Stack Overflow stat,
jobkorea.co.kr's May-2026 side-project survey — 26.2% stopped mid-build,
23.8% not started, 16.7% not doing anymore — and 10xbuilders.io's "왜
99%가 망하는가") and were excluded: all either duplicate angles already
used this week, or (jobkorea specifically) a Korean-source survey whose
precise percentages could not be verified against the source — same
caution applied on 2026-08-20 when this exact source last surfaced;
jobkorea.co.kr remains EGRESS_BLOCKED for WebFetch.

Fresh, unused, and verifiable-via-search-snippet angle: a distinct genre
of sites has emerged that exists purely to catalog dead AI products —
"AI Graveyard" trackers. This is a different angle from every prior post
this week (which discussed abandonment directly); this one is about the
fact that *cataloging deaths* has become its own content category,
and argues the gap between what these trackers record (just the fact
of death) and what's actually salvageable (code, domain, early users)
is the opening for a handover instead of a grave-listing. Sources
attempted via WebFetch and all EGRESS_BLOCKED (dang.ai, boingboing.net,
www.taskade.com, tooldirectory.ai) — consistent with every prior day
this week — so this run relied on WebSearch result snippets only,
treated as summary-only, no verbatim text copied into the post. Two
sources anchor the flagship's cited figures:
- AI Graveyard: 142 AI companies so far buried, many more to come —
  Boing Boing — https://boingboing.net/2026/05/07/ai-graveyard-142-ai-services-so-far-dead-many-more-to-come.html
- The Vibe Coding Graveyard: 14 AI Tools That Died (2026) — Taskade
  Blog — https://www.taskade.com/blog/vibe-coding-graveyard

A third figure (dang.ai tracking 1,757 dead AI listings; ~40% of the
~14,000 AI startups launched in 2024 shut down by early 2026) surfaced
via WebSearch snippet only (attributed to dang.ai/dev.to commentary);
the underlying page itself was not directly fetchable, so it's cited in
the post as a general figure without a direct source link in the
wa-source block, consistent with the "no verbatim reproduction, but
also no over-claiming a link we couldn't verify" convention used in
prior runs (e.g. 2026-08-21's Black Duck OSSRA figure).

Other items surfaced but not used (adjacent, not duplicated, kept as
backlog): Flippa/Acquire.com 2026 marketplace-growth statistics
(SaaS-resale trend, not really an abandonment angle — more a
competitor-marketplace topic, deliberately not centered), "Killed by
AI" (106 dead products) and "Killed by OpenAI" trackers (same genre as
the flagship, folded into the general framing rather than each cited
individually), GitHub Wrapped 2025 backlog-humor content (too thin to
anchor a full post on its own).

## SEO WRITER

Flagship: `public/blog/ai-graveyard-vs-handover.html` — "AI 프로젝트
무덤만 기록하는 사이트가 늘고 있습니다". Angle: an emerging genre of
sites (Boing Boing's "AI Graveyard," Taskade's "Vibe Coding Graveyard,"
Killed by AI, Killed by OpenAI, dang.ai's graveyard tracker) exists
purely to record that AI products died — but recording a death is not
the same as recognizing what's still usable inside it (code, domain,
early users). This is a genuinely new framing versus the last 7 days of
posts (buyer demand, 60%-abandonment stat, flip-price numbers, DIY
broker signal, GitHub-graveyard confession genre, domain-only vs.
built-asset, zombie/still-live-but-unmaintained) — none of the prior
posts discussed the "cataloging deaths as content" phenomenon itself.
Followed the template structure exactly (kicker → H1 → 3-bullet issue
summary + source links → insight → practical tips → CTA → disclaimer),
reused `blog.css` and the current site-wide asset versions unchanged
(`styles.css?v=20260816-photoback`, `blog.css?v=20260729-deep-qa`,
`yard-theme.css?v=20260729-deep-qa`, `theme-preview.js?v=20260816-ch1`)
since no CSS/JS was touched — confirmed these were still the versions
in use on `blog/index.html` before writing, since a same-day homepage
change (히어로 스크롤 월드, unrelated to this pipe) landed on 2026-08-21
after that day's SEO commit. Added a card to the top of
`public/blog/index.html` (thumb class `t6`, tied for least-used of the
8 rotation classes going in) and a `<url>` entry to
`public/sitemap.xml` (lastmod 2026-08-22, same priority/changefreq
convention as the other daily posts).

## REVIEW

Checked against the AGENT_TEAM.md checklist, programmatically where
possible:
- No earnings/success/sale guarantee language — grepped "보장" in the
  new file: exactly 1 hit, inside the disclaimer explicitly saying
  outcomes are *not* guaranteed.
- Brand shown only as English "WakeAgain" — grepped "웨이크어게인":
  0 hits.
- CTAs point to allowed wakeagain.com paths only (`/sell.html`,
  `https://wakeagain.com`, `/guide/how-to-sell.html` — confirmed the
  file exists on disk before linking).
- No RoadLog / ReachKit mentions (grepped, case-insensitive: 0 hits
  each).
- No verbatim reproduction of the two source articles — summary + link
  only, both in the `wa-source` block and in prose; only had WebSearch
  snippets to work from (both source domains were egress-blocked), so
  content was paraphrased from those snippets, not copied from the
  pages.
- Title/slug checked against all existing `public/blog/*.html` <title>
  values programmatically (dict-based dup check) — no duplicate
  (`ai-graveyard-vs-handover` is new).
- Disclaimer paragraph present (grep count: 1).
- HTML tag balance checked programmatically with `html.parser` for both
  the new post and the edited `blog/index.html` — residual open-tag
  stack empty for both. `sitemap.xml` re-parsed with
  `xml.etree.ElementTree` — well-formed, 48 `<url>` entries.
- Also scanned the new file for stray/mis-encoded characters
  (codepoint sweep outside the Hangul/ASCII ranges) — none found.

PASS on the first pass — no Writer corrective round needed.

## PUBLISH

Session container started with a detached HEAD, one commit behind
origin/master (same pattern as 2026-08-21's log — local `master` ref
existed but wasn't checked out). Ran
`git fetch origin master && git checkout -B master origin/master`
first — no conflict, branch matched origin/master exactly after the
fetch. Checked `.claude/ACTIVE_WORK.md` per the repo's concurrent-work
rule before touching files — no active occupation from Cowork,
declared this session's own occupation line
(`public/blog/**, public/sitemap.xml`) for the duration of the work,
then cleared it once the content push completed. Staged only
`public/blog` and `public/sitemap.xml` (not `git add -A` —
`.claude/ACTIVE_WORK.md` was deliberately excluded from that commit and
left for this log commit), confirmed `git status --short` showed
exactly the 3 intended files before committing. Pushed as `7d2fda6`
("content: daily WakeAgain SEO 2026-08-22") — first try, no retry
needed, no 403 permission issue.

## TESTING

Installed `requirements.txt` into a fresh venv
(`/tmp/wa_venv_seo_20260822`, avoids the container's system Python
state) and ran `_smoke_check.py`. Hit the same pre-existing failure
documented in every prior daily-SEO log this week
(`sqlite3.OperationalError: no such table: projects`, in
`/api/v1/stats`, on a fresh empty-DB container) — unrelated to this
diff; every page/API check that ran before that point passed, including
`/sitemap.xml`, `/`, `/sell.html`, `/robots.txt`, `pretty 404`,
`/health`, `/app/`. Additionally verified the three changed/added files
directly via a Starlette `TestClient` against an isolated `DATA_DIR`:
`/blog/ai-graveyard-vs-handover.html` → 200, `/blog/index.html` → 200,
`/sitemap.xml` → 200.

## LIVE

Could not be verified from this container — `wakeagain.com` is denied
by the session's egress policy (same limitation noted in every prior
daily-SEO log this week; this run additionally found `dang.ai`,
`boingboing.net`, `www.taskade.com`, and `tooldirectory.ai` also
blocked for WebFetch, which shaped the research sourcing above to
WebSearch-snippet-only). Railway auto-deploys on push to master;
confirm https://wakeagain.com/blog/ai-graveyard-vs-handover.html
manually.
